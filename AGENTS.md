# Agro Weekly — AI 작업 규칙

먼저 `README.md`를 읽고 현재 Git 상태를 확인합니다.

- 이 폴더가 Agro Weekly의 유일한 운영본입니다.
- 평문 앱 `app.html`, 접근 설정 `.site-access.json`, 이메일 환경 파일은 Git에
  커밋하지 않습니다.
- 발행 전 암호화와 `scripts/verify_weekly_deploy.js` 검증을 완료합니다.
- 메일 발송과 운영 배포는 사용자의 명시적 요청이 있을 때만 실행합니다.
- 공식 운영 주소와 GitHub 원격 저장소를 임의로 변경하지 않습니다.
